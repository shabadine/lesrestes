<div class="text-center py-5 bg-light rounded shadow-sm">
    <i class="bi {{ icon }} text-secondary" style="font-size: 4rem;"></i>
    <h3 class="mt-3 text-dark">{{ title }}</h3>
    <a href="{{ link_path }}" class="btn {{ btn_class|default('btn-success') }} mt-2">{{ btn_label }}</a>
</div>